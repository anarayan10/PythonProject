'''tuples are immutable cant be changes
and it is definded with ()brackets'''

#we cannot change the tuple but can only get information about it
#example :
numbers = [1, 2, 3]
numbers.count(3)
numbers.index(3)
print(numbers.count(3))
print(numbers.index(2))

